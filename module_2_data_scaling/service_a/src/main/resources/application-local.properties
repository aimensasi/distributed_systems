
spring.datasource.url=jdbc:postgresql://localhost:5433/lab_db

replica.datasource.url=jdbc:postgresql://localhost:5434/lab_db
replica.datasource.username=lab_user
replica.datasource.password=lab_pass

management.endpoints.web.exposure.include=health,metrics,prometheus
management.tracing.sampling.probability=1.0


management.opentelemetry.tracing.export.otlp.endpoint=http://localhost:4318/v1/traces
# Jaeger only supports traces, not metrics - disable OTLP metrics export to avoid 404
management.otlp.metrics.export.enabled=false

spring.data.redis.host=localhost
spring.data.redis.port=6380
spring.data.redis.database=0

# HikariCP — intentionally tiny pool to make exhaustion obvious
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=3000
spring.datasource.hikari.leak-detection-threshold=2000